# Frontend Environment Variables
# Copy this file to .env and update with your actual values
# Command: cp .env.example .env  (Linux/Mac)
# Command: copy .env.example .env  (Windows)

# API Configuration
# Backend API URL
# Development: http://localhost:5000
# Production (Vercel): /api (relative URL, same domain)
# Production (Render/separate backend): https://your-backend-service.onrender.com
VITE_API_URL=http://localhost:5000

# Push Notifications - VAPID Public Key (Optional)
# This is the PUBLIC key for Web Push Notifications
# Must match the VAPID_PUBLIC_KEY in backend/.env
# Generate VAPID keys using: npm install -g web-push && web-push generate-vapid-keys
# See PUSH_NOTIFICATIONS_SETUP.md for detailed instructions
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key-here
